/*
 * Use this with the v0.5 of the FOOL Language.
 * Here you can test type checking.
 */
let
  var x:int = 5+3;
  fun f:bool (n:int, m:int)
    let
      var x:bool = true;
    in x==(n==m);
  var a: bool = 0;
  var b: bool = 1;
in
  print (
   if f(a,b)
     then { false }
     else { 10 }
  )
;
